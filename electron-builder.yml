appId: com.hinahina.realize-cam
productName: Realize Virtual Camera
directories:
  buildResources: build
files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!dist*/**"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{.env,.env.*,.npmrc,pnpm-lock.yaml}"
  - "!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}"
asarUnpack:
  - resources/**
  - node_modules/vcam-napi/**
extraResources:
  - from: scripts
    to: scripts
  - from: resources/animations.enc
    to: animations.enc
  - from: ../vcam/build/service/Release/vcam-service.dll
    to: vcam-service.dll
  - from: ../vcam/build/driver/Release/vcam-driver.dll
    to: vcam-driver.dll
win:
  executableName: RealizeCam
  requestedExecutionLevel: requireAdministrator
  forceCodeSigning: false
  sign: null
  signAndEditExecutable: false
  target:
    - nsis
    - zip
nsis:
  artifactName: realize-cam-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  include: build/installer.nsh
  oneClick: false
  perMachine: true
  allowToChangeInstallationDirectory: true
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
